# 1. 보안 환경변수 (필수 제외)
.env
.env.local

# 2. 의존성 모듈 (npm install로 설치되는 폴더)
node_modules/

# 3. 빌드 결과물 (자동 생성되는 js/css 파일 폴더)
dist/
build/

# 4. 특정 자동 생성 JS 파일만 제외할 경우 (예시)
bundle.js
*.min.js